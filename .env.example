VITE_AI_BACKEND_URL=http://127.0.0.1:8787
# Backend CORS allow-list (comma-separated). MUST be set for cross-origin
# access — empty means the API refuses every cross-origin request.
ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
