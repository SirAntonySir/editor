# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
release
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?


.env
.claude/*
.agents/*

# Backend
backend/.env
backend/.venv/
backend/**/__pycache__/
backend/**/*.pyc
backend/.pytest_cache/
backend/models/
backend/.sessions/
# Tests run with cwd=backend so the relative SESSIONS_DIR resolves to
# a doubly-nested backend/backend/.sessions/ if a test isn't isolated
# via tmp_path. Ignore both shapes.
**/.sessions/
.superpowers/

# Experimental / in-progress files
*.future

# Worktrees (created by superpowers:using-git-worktrees)
.worktrees/

# Vendored ML model + runtime assets (populated via `make download-sam`)
public/models/
public/ort/
