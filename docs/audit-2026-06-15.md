# Editor Project — Consolidated Audit (2026-06-15)

**Scope:** 404 TS files (`src/`), 247 Python files (`backend/`), shaders, workers, shared schemas, electron, build config.
**Method:** 8 parallel Explore agents, one per slice; results cross-referenced + deduped.
**Totals:** ~165 distinct findings → 14 Critical, 26 High, 49 Medium, 76 Low.

Severity rubric:
- **Critical** — can crash, corrupt, leak unbounded, or break SSoT.
- **High** — wrong results / strong perf cliff / large-scale duplication.
- **Medium** — edge-case bug or noticeable perf.
- **Low** — polish / hygiene / micro-opt.

Status legend:
- `[x]` resolved (commit SHA noted)
- `[~]` partially resolved (notes inline)
- `[ ]` open

**Progress snapshot:** 14 Critical → 12 fully resolved, 2 partial, 0 open. 26 High → 15 resolved (11 open). Medium & Low buckets largely open; a handful of Low items landed in the mechanical wave.

---

## 🟥 CRITICAL — fix before further feature work

- [x] **C1. SSE EventBus has unbounded queues + leaks queues on hard disconnect** — resolved in `83a3668`
  - `backend/app/state/events.py:20` — `asyncio.Queue()` created with **no `maxsize`**. Long sessions or chatty publish paths grow memory without bound; `put_nowait` never backpressures.
  - `backend/app/state/events.py:16, 25-29` — Queue refs leak in `_queues[session_id]` if a client dies without graceful close (kill -9, network blip). Slow leak per reconnect.
  - `backend/app/api/state.py:199-221` — cleanup only runs if the async generator is GC'd; not guaranteed.
  - **Fix landed:** `maxsize=1000`; on QueueFull drain + inject synthetic `state.gap`; empty subscriber buckets pruned on unsubscribe.

- [x] **C2. Persistence writes per-node image data into every checkpoint** — resolved in per-node cluster (`e208889`)
  - `backend/app/session/persistence.py:36-39` — `_EXCLUDE_FROM_PERSIST` only excludes the *singleton* `image_bytes` / `prepare_result`, **not** `image_bytes_by_node` or `prepare_result_by_node`. Multi-image-node sessions balloon the JSON by MB per checkpoint.
  - **Fix landed:** `_EXCLUDE_FROM_PERSIST` extended with `image_bytes_by_node`, `mime_type_by_node`, `prepare_result_by_node`.

- [x] **C3. Undo/redo silently drops per-node image context** — resolved in per-node cluster (`831c7d0`, `d9a7a63`)
  - `backend/app/session/history.py:36-69` — `Snapshot.capture()` predates the per-node refactor; doesn't capture `image_context_by_node` or `prepare_result_by_node`.
  - `backend/app/state/document.py:94-122` — `apply_snapshot()` restores canonical/widgets/masks but ignores the same per-node fields → undo wipes analysis context on multi-image docs.
  - **Fix landed:** `Snapshot` carries `image_context_by_node`; `apply_snapshot` restores it + clears the legacy singleton; `history.applied` event payload extended with `imageContextByNode`. Frontend consumer side is a separate one-line follow-up.

- [x] **C4. Synchronous Anthropic call on the asyncio loop** — resolved in `83a3668`
  - `backend/app/api/analyze.py:386` — REST `/analyze` calls `client.analyze_image()` (sync, 30–120 s) without `run_in_executor` / `asyncio.to_thread`. **Blocks the entire event loop** — all SSE, tool calls, health checks freeze for the duration.
  - `backend/app/services/sam_client.py:42-46` — SAM model load (`SAM2ImagePredictor.from_pretrained`, multi-GB) also runs sync inside an async handler on first use.
  - **Fix landed:** `/analyze` + `/name-region` route the sync Anthropic + SAM work through `asyncio.to_thread`. The lazy SAM model load on first call is still sync inside the worker thread; a follow-up could eager-load at startup.

- [x] **C5. REST tools endpoint has zero rate limiting** — resolved in `83a3668`
  - `backend/app/api/tools_rest.py:17-23` — `/api/tools/{name}` bypasses the 30/min throttle that the MCP path enforces (`mcp/server.py:93`). Cheap DoS or expensive-tool hammering.
  - **Fix landed:** moved `_rate_limiter` to `deps.get_tool_rate_limiter()`; REST + MCP share one bucket per session; REST returns 429 on overflow.

- [x] **C6. Sessions on disk never pruned** — resolved in `83a3668`
  - `backend/app/services/session_store.py:195-201` — `prune_disk()` defined but **never called anywhere** in the codebase. Sessions accumulate forever (with up to 2 MB image bytes each).
  - **Fix landed:** `main.py` lifespan task runs `prune_disk()` every `disk_prune_interval_s` (default 1 h) for entries older than `disk_session_max_age_s` (default 7 d).

- [~] **C7. setState-during-render in two components** — partial resolution in `977bedc`
  - [x] `src/components/inspector/adjustments/HslPanelView.tsx:38-40` — `setBand(bands[0].key)` in render. **Fix landed:** replaced with a derived `activeBand`.
  - [~] `src/components/workspace/ImageNode.tsx:87-90` — `setPrevChromeVisible` + `setCompareHeld` in render body. **Kept as the documented previous-prop pattern** (React docs explicitly endorse this; the `useEffect` form trips the cascading-renders ESLint rule). Comment expanded to make the choice explicit.
  - [~] `src/components/CommandPalette.tsx:180-184` — same pattern, kept for the same reason. No code change.

- [~] **C8. Stale-closure / cross-store mutation hazards in SSE event handler** — partially resolved
  - [x] `src/store/backend-state-slice.ts:243` — `state.gap` handler captures `sid` then fires `useBackendState.getState().setSnapshot()` in a fire-and-forget async closure. No re-check that the session is still active → wrong-session writes after a reset. **Fix landed (`8d92942`):** the closure now re-checks `useBackendState.getState().sessionId === sid` before writing; logs and drops on mismatch. Regression test asserts the late refetch is dropped when sessionId changes mid-fetch.
  - [x] `src/store/backend-state-slice.ts:323-325` — Inside an Immer producer, calls `useEditorStore.getState().consumePinRequest()` / `setPinnedWidgetParams()`. Cross-store mutation from a state reducer; if either store resets mid-event, state diverges. **Fix landed (`8d92942`):** `applyEvent` now uses a side-effects-queue pattern — cross-store calls and `tetherWorkspaceWidget` are pushed to a local array inside the Immer producer and drained AFTER `set(...)` returns. The H4 cluster's `useSuggestionsUi.markPending` bridge was moved to the same queue.
  - [~] `src/core/document.ts:243-246` — `openImage()` triggers `useAiSession.getState().reset() / openSession()` in a deferred microtask after committing user-visible state. Open-image-then-open-image races. **Partial fix landed (`3cf57f3`):** hoisted the dynamic `import('@/hooks/useImageContext')` to a static top-of-file import; eliminates the import-resolution race window. The second race window — `useAiSession.openSession` is async (uploads the offscreen canvas) and a second openImage call mid-upload can clobber the in-flight session — needs a generation counter on `useAiSession.openSession` and is deferred to its own cluster.

- [x] **C9. Two SSE/REST race windows because the document write-lock isn't held** — resolved
  - [x] `backend/app/api/state.py:60-66` — `/state/{sid}` computes a snapshot **without** `store.with_document_lock(sid)`. Torn read possible while a tool is mid-mutation. **Fix landed (`09f935d`):** `state_snapshot` now wraps `compute_snapshot(doc)` in `with _store().with_document_lock(sid) as doc:`. Regression test asserts the lock is acquired.
  - [x] `backend/app/api/state.py:176-198` — initial replay events for SSE captured outside the write lock; the inline comment claims otherwise. **Fix landed (`09f935d`):** `state_events` now wraps both the `bus.subscribe(sid)` call and the `doc.history` replay capture in the document write lock — atomic with respect to any concurrent mutator. Inline comment rewritten to reflect actual behaviour. Regression test asserts the lock is acquired.
  - Bonus: `get_mask_bytes` had the same pattern (read `doc.masks` outside the lock). Wrapped while we were here (`09f935d`); regression test included.

- [x] **C10. Race + lost-update on compound widget edits** — resolved
  - [x] `backend/app/tools/widgets/set_widget_param.py:82-83` — two concurrent set-param calls on different keys of the same compound widget both read empty `locked_params`, both append, second overwrites first. Lock state lost. **Audit framing didn't apply:** `set_widget_param` is `kind="mutate"`, so the tool registry already serialises calls per session via `with_document_lock` (`tools/registry.py:117`). Two concurrent calls on the same session queue; no in-process race. Documented in the handler's doctrine comment (`2744956`) so future readers don't reintroduce the concern.
  - [x] Same file 44-55: if `node` is None (binding exists, target node deleted), the canonical write is silently skipped → widget vs. op-graph divergence. **Fix landed (`2744956`):** now raises `_OrphanBinding` BEFORE any state mutation; mapped to a new `orphan_binding` error envelope code. Tests at `backend/tests/tools/widgets/test_set_widget_param_orphan.py` cover (a) the raise, (b) the state-invariant that no binding/canonical mutation occurs on the orphan path, and (c) the happy path still works (`83c93b6`).

- [x] **C11. Premultiplied-alpha / luma-coeff drift in WebGL pipeline** — resolved in `b48dea8`
  - `src/shaders/split-tone.glsl.ts:27` — uses BT.601 `(0.299, 0.587, 0.114)` luma; every other shader (basic-adjustments, clarity, sharpen, histogram-compute, lut-parser) uses BT.709 `(0.2126, 0.7152, 0.0722)`. Visible threshold drift on saturated images.
  - **Fix landed:** split-tone now uses BT.709 too.

- [x] **C12. Layer compositor recurses without a cycle guard** — resolved in `b48dea8`
  - `src/lib/layer-compositor.ts:68-113` — `renderLayer()` follows `parentLayerId` recursively. Malformed/self-referencing tree → infinite recursion → tab crash. No depth cap, no `Set<seen>`.
  - **Fix landed:** `renderLayer(layer, seen = new Set())` bails with a console warning when a cycle is detected.

- [x] **C13. Frontend reloads source texture every frame** — resolved in `b48dea8`
  - `src/shaders/pipeline.ts:416-432` — `setSource()` unconditionally `gl.deleteTexture` + `texImage2D`. On a 4K image, that's ~100 MB/frame re-upload even when only an adjustment param moved. Dominates GPU upload time.
  - **Fix landed:** `setSource(source, dirty=true)` reuses the same `WebGLTexture`; skips upload entirely on `dirty:false` + same identity.

- [x] **C14. CORS default leaks dev origins to prod** — resolved in `83a3668`
  - `backend/app/config/env.py:18` — `allowed_origins` default is `http://localhost:5173,http://127.0.0.1:5173`. If an operator forgets to override, a co-located dev server would be a credential-leak vector.
  - **Fix landed:** default empty + startup warning; `.env.example` documents the dev value; `backend/.env` appended with the dev value so the workflow keeps working.

---

## 🟧 HIGH

### Correctness

- [x] **H1** — `src/hooks/useCanonicalParam.ts:30-52`, `src/lib/use-processing-param.ts:24-82`, `src/lib/use-graph-adjustment.ts:16-61`, `src/lib/use-adjustment.ts:21-77` — **four debounced param hooks all missing `useEffect` cleanup** for their `timer`/`debounceRef`. Component unmounts mid-drag → callback fires on dead session. **Fix landed in `977bedc`:** `useEffect(() => () => clearTimeout(...), [])` added to all four.
- [ ] **H2** — `src/store/backend-state-slice.ts:261-263` — `history.applied` partially merges `operationGraph`/`widgets`/`masksIndex` from the backend payload; if any field is absent the snapshot becomes a hybrid of two revisions.
- [ ] **H3** — `src/store/backend-state-slice.ts:286-293` — `context.updated` constructs a synthetic minimal snapshot with `id:''` and empty arrays; never validated; subsequent events merge into a malformed root.
- [x] **H4** — `src/store/backend-state-slice.ts:352, 357` vs. SSE `widget.accepted` — **two acceptance paths**: SSE removes the widget; frontend `addAcceptedSuggestion` keeps it. Double-render risk. **Fix landed:** the SSE handler's `acceptedSuggestions.add(id)` line was always redundant — by the time backend confirms acceptance, the FE has already added the widget via either `SuggestionChips.handleAllow` (user click) or `useAutoTetherAiSuggestions` (auto-tether on session resume). Dropped that line; the SSE handler now only filters the widget out of the snapshot. The two "paths" framing dissolves into one: FE marks engagement, backend confirms via snapshot mutation.
- [ ] **H5** — `src/store/layer-slice.ts:88` — `Object.assign(layer, updates)` inside Immer is non-idiomatic; works today, brittle to future patterns.
- [ ] **H6** — `src/store/workspace-slice.ts:288` — `setEdge({...edge})` is shallow; `edge.scope` (union) is shared. Mutation by caller leaks into store.
- [ ] **H7** — `backend/app/session/history.py:169-172` — coalesce mutates `tip.after` in place; safe today but a sharing bug waiting to happen.
- [x] **H8** — `backend/app/session/persistence.py:38-39` + `revive.py:36-58` — `prepare_result` excluded from persistence with "regenerable" promise, but **no regenerate-on-revive guard** and no staleness check. Callers get None and crash. **Fix landed in per-node cluster (`e208889`, `7f25cc4`):** persistence and snapshot both exclude `prepare_result_by_node`; the regen-on-demand contract is documented in `document.py` + `persistence.py` + `history.py` docstrings; callers either hold a value or trigger `PrepareImageTool` lazily.
- [x] **H9** — `backend/app/state/document.py:40-59` — singleton↔per-node `image_context` migration is half-done; both code paths still in use, no sync. Last-writer-wins, with persistence seeing only one side. **Fix landed in per-node cluster (`c320538..ae701bc`):** writers per-node-only; `_promote_singletons_to_per_node()` runs on every revive AND fresh `_new_document`; doctrine documented in 4 module docstrings; suite of round-trip + promotion tests.
- [x] **H10** — `backend/app/services/anthropic_client.py:469-509` — retries only `ValidationError`; `APIConnectionError`/`RateLimitError`/`APIStatusError` aren't retried; original exception masked by generic `RuntimeError`. **Fix landed in `83a3668`:** `_messages_create` helper retries `APIConnectionError` / `APITimeoutError` / `RateLimitError` / 5xx `APIStatusError` with 0.5 s → 1.0 s backoff; all 14 SDK call-sites routed through it; final `RuntimeError` raises preserve cause via `from`.
- [ ] **H11** — `backend/app/services/autonomous_suggestions.py:11-211` — **no debounce / no in-flight guard**. A rapid SSE storm triggers concurrent Anthropic resolvers; cost amplification + race on widget IDs.
- [ ] **H12** — `backend/app/api/state.py:189-196` — `state.gap` calc reads `doc.history` across the prune/publish race in `tools/registry.py:190-208`; clients can be told to gap-reload when the data was actually available.

### Performance

- [ ] **H13** — `src/hooks/useImageNodeRender.ts:65-87` — selector includes the optimistic `Map`, which Immer recreates on every `applyOptimistic`. Effect re-fires on **every slider tick**. Combined with 24-dep array (242-268) it cascades a full composite per pixel.
- [x] **H14** — `src/shaders/pipeline.ts:316-345` — Curves shader allocates **4 fresh 256×1 RGBA textures every frame** (and deletes them). ~240 alloc/dealloc/s with curves active. **Fix landed in `b48dea8`:** persistent `curvesLutTextures` cache keyed by `(adjustmentId, channel)`; shared identity LUT allocated once and bound for inactive channels; freed in `clearLutCache` / `dispose`.
- [x] **H15** — `src/shaders/pipeline.ts:111-124` — `resizeFBOs()` deletes/recreates all four FBOs unconditionally on any size delta; blocks main thread 10–50 ms at 4K. **Fix landed in `b48dea8`:** FBO textures resized in place via `texImage2D` on the existing handles.
- [x] **H16** — `src/lib/image-node-renderer.ts:252-254` — downscaled scratch canvas not memoised by `(imageNodeId, renderScale)`; redrawn every frame. **Fix landed in `b48dea8`:** new `getMemoisedScratchCanvas(imageNodeId, source, w, h)` short-circuits the per-frame downscale; cache cleared by `clearInternalCanvasCache`.
- [ ] **H17** — `backend/app/state/document.py:68-69` — history list unbounded between user actions; checkpointer serialises the whole growing structure every 5 s.
- [ ] **H18** — `backend/app/tools/fused/__init__.py:48-99` — `list_fused_templates()` re-instantiates every template (including 8 per-band `ToneBandTemplate`) on every call; called from `list_fused_tools`, `refine_widget`, `repeat_widget`.
- [ ] **H19** — `backend/app/tools/widgets/propose_stack.py:384-407` — `asyncio.gather` over op resolvers with **no per-op timeout**; one hung Anthropic call hangs the entire stack proposal.

### Duplication / Architecture

- [x] **H20** — **Four duplicated param hooks** (`useCanonicalParam`, `use-processing-param`, `use-graph-adjustment`, `use-adjustment`) — same shape (derive node → read optimistic/widgets/op-graph → debounce write); only differ in routing target. **Fix landed:** `useGraphAdjustmentParam` + `useAdjustmentParam` were dead code and were deleted (`7ee9651`); `useCanonicalParam` + `useProcessingParam` now forward to a single `src/lib/use-param.ts` core (`eeb1043`, `c520261`, `bde4617`, `8616011`). Debounce / optimistic-patch / stale-write-guard logic lives in one file. **Followup spotted during senior review:** `src/components/inspector/adjustments/RegistryDrivenSectionBody.tsx` re-implements the same debounce + optimistic-patch logic by hand at module scope (toolrail path, lines 189-218 — missing the stale-write guard) and runs the widget path (lines 95-105) with NO optimistic patch + NO debounce + NO guard. **Widget-path slice landed in `2ce7234`:** `WidgetShell.setParam` now coalesces `set_widget_param` writes per `(widget, paramKey)` on a 100ms timer (optimistic patch was already there); `backendTools.invokeTool` returns a soft-fail envelope on HTTP 429 so fire-and-forget callers don't surface "Uncaught (in promise) Error" spam on rate-limited ticks. The `RegistryDrivenSectionBody` toolrail path's missing stale-write guard is the still-open piece of this follow-up.
- [x] **H21** — **17+ fused-tool files share the same skeleton** in `backend/app/tools/fused/*.py` (`_RESPONSE_SCHEMA`, identical `resolve()` calling `anthropic.resolve_fused_tool`). **Fix landed:** research showed only 9 of 17 files actually had the override; the base `FusedToolTemplate.resolve()` already auto-generated the same `_RESPONSE_SCHEMA` from `param_envelope.keys()`. Extended the base with dotted-path `context_inputs` support (`3994691`); deleted 4 overrides cleanly — `exposure_balance` (`8afc421`), `cast_correct` (`fc88cc8`), `teal_orange` (`ef48d68`), `subject_pop` (`073a0dc`); aligned `context_inputs` + deleted overrides in `warm_grade` (`d89949d`), `cool_grade` (`448ba32`), `bw_cinematic` (`15382ff`). 2 files kept their overrides as legitimate special cases — `sky_recovery` filters region_stats by `is_sky_likely` AND renames the container key to `sky_swatches`; `portrait_glow` renames the container key to `skin_regions`. Net: ~370 lines of duplicated code removed; 7 of 9 overrides gone.
- [x] **H22** — `src/lib/toolrail-spawn.ts:26-44` vs. `:65-80` — identical active-image-node + layer resolution logic in two functions in the same file. **Fix landed in `2a88a57`:** `spawnToolWidget` now delegates session + active-image-node + layer + scope resolution to the pre-existing `_resolveSpawnContext` (already used by `spawnRegistryOp` / `spawnRegistryPreset`). ~25 lines of duplicated gate logic removed; toast + bail behaviour already covered by existing tests at `toolrail-spawn.test.ts:38` and `:81`.
- [x] **H23** — Two registries with overlapping op metadata: `backend/app/registry/loader.py` ↔ `backend/app/engine/registry.py` (`ENGINE_OPS`) ↔ `backend/app/tools/tool_defaults.py`. `TOOL_DEFAULTS` is acknowledged debt — only LUT/filter still hand-written. **First wave:** `tool_defaults.py` deleted (`21db7d5`); the lone `filter` entry moved inline into `propose_stack._handle_filter_spawn` (`897ef86`). **Engine view collapsed:** `effective_tool_defaults` + `param_label` accessor helpers promoted into `app/registry/loader.py` (`7e225ce`); the two test/script consumers migrated to walk `get_registry().ops` directly (`f6c64b2`, `28728a8`); the deleted `tests/engine/test_registry.py`'s shape assertions rehomed into `tests/registry/test_registry_shape.py` (bundled into `d6a197c` by parallel commit); `backend/app/engine/` deleted entirely (`527c2b2`). No production consumers of `ENGINE_OPS` remained — the "two registries" overlap is closed.
- [x] **H24** — `backend/app/tools/widgets/propose_widget.py` is now a deprecated fast-path that only handles `tool_invoked` for LUT/filter, but coexists with `propose_stack._handle_tool_invoked()` doing nearly the same work. Two entry points to widget spawning. **Fix landed:** `propose_widget.py` deleted (`21db7d5`); the LUT/filter spawn moved to `propose_stack._handle_filter_spawn` (`897ef86`); the one frontend caller (`src/tools/filters-tool.tsx`) migrated to `propose_stack(forced_ops: ['filter'])` (`f843b1c`). Single entry point now.
- [x] **H25** — Inspector-folder primitives imported from `widget/` folder: `HslWidgetBody`, `LevelsWidgetBody`, `CurvesWidgetBody`, `InfoWidgetShell` all pull from `@/components/inspector/...`. Topic-folder rule breach per CLAUDE.md. **Fix landed in 6 slices:** `AdjustmentSlider` → `ui/` (`cc603cb`, 12 consumers); `MetricChip` + `MetricChipGrid` → `ui/` (`1822344`); `LevelsHistogramControl` → `ui/` (`0ffbaa0`); `CurveEditor` primitive → `ui/` (`534549e`); `BindingRow` + the entire `inspector/widget/primitives/` folder (`ToggleControl`, `ChoiceControl`, `ColorControl`, `RegionPickerControl`, `MaskThumbnailControl`, `CurveControl`, plus dead `SliderControl`) → `widget/` (`ff2bb71`); `HslPanelView` + `HslSingleBandView` + 3 supporting primitives + `hsl-bands` data → `widget/hsl/` (`b266942`). Final verification: `grep -rln "from '@/components/inspector" src/components/widget/` returns empty; `ui/` doesn't import from `widget/` or `inspector/`. The `widget/ → inspector/` direction is gone. `inspector/widget/PreviewThumbnail.tsx` is dead code — deletion deferred.
- [ ] **H26** — Three node-type vocabularies overlap and aren't clearly delimited: `src/types/operation-graph.ts` (backend protocol) ↔ `src/types/workspace.ts` (UI canvas) ↔ `src/types/graph.ts` (graph-editor). Causes mis-imports.

---

## 🟨 MEDIUM

### Correctness

- [ ] `src/hooks/useBackendSession.ts:82-208` — subscription ref overwrite race on rapid `sessionId` change → orphan SSE subs.
- [ ] `src/hooks/useMobileSam.ts:8, 52-90` — global `_cache` Map never auto-invalidated on pixel replacement; depends on every replacement path calling `clearMobileSamCache`. Easy to miss.
- [ ] `src/hooks/useAutoTetherAiSuggestions.ts:17-47` — double-check between selector and effect can double-tether; viewport read at line 35-44 is synchronous from React Flow, can race zoom.
- [ ] `src/store/backend-state-slice.ts:407-409` — `s.optimistic.delete()` during iteration; works but error-prone; also no upper bound on patch accumulation.
- [ ] `src/store/selection-slice.ts:44-50` — `sortByPixelCount` iterates every mask's Uint8Array per candidate; O(candidates × mask_bytes).
- [ ] `src/core/document.ts:120-144` — `newDocument()` doesn't `resetWorkspace()` like `closeDocument` does → orphaned image nodes/edges.
- [ ] `src/core/history.ts:14-22` — `stack.slice(0, cursor+1)` on every push; O(cursor); also no max-size on the redo tree.
- [ ] `src/core/mask-store.ts:78-83` — `allForLayer` linear scan; no reverse index.
- [ ] `src/lib/layer-compositor.ts:103-106` — layer-mask alpha multiplication assumes straight-alpha; premultiplied sources go dim.
- [ ] `src/lib/layer-compositor.ts:115-125` — `isCompositing` re-entry guard returns stale canvas silently to listeners.
- [ ] `src/shaders/pipeline.ts:86-88` — context `premultipliedAlpha: false` but `blend.glsl.ts` mixes assume straight alpha while Canvas-2D compositing rules differ. Fringing at opacity<1 + non-normal blend.
- [ ] `src/shaders/pipeline.ts:206-266` — clarity hard-codes `radius = 0.5`; backend param ignored.
- [ ] `src/lib/pipeline-manager.ts:42-52` — RAF coalesce drops snapshot version; rapid-succession backend updates lose intermediate state.
- [ ] `backend/app/state/operations.py:22-24` — `MaskScope → GraphScope` drops `mask_id`; frontend must reconstruct from `widget.scope`; divergence-prone.
- [ ] `backend/app/schemas/widget.py:353` — `Widget.op_id` accepts both `op_id` and `fused_tool_id` via `AliasChoices`; ambiguous if both present.
- [ ] `backend/app/schemas/widget.py:394-397` — `MaskRecord.image_node_id: str | None` for back-compat; a dangling mask in a widget crashes the renderer.
- [ ] `backend/app/state/document.py:268-291` — `gc_dismissed_widgets()` reads `self.history[0]` without checking `if not self.history` → IndexError when fully pruned.
- [ ] `backend/app/tools/widgets/set_widget_param.py:67-76` — narrowing on `op.compound` then re-using without second guard → potential AttributeError.
- [ ] `backend/app/tools/widgets/propose_widget.py:136-156` — `binding.default` not set on the TOOL_DEFAULTS path; later refine snaps to wrong default.
- [ ] `backend/app/tools/atomic/combine_masks.py:61-62` — shape mismatch reported as `_UnknownMask`; wrong exception type.
- [ ] `backend/app/api/refine.py:18-51`, `backend/app/api/panel.py:17-69` — deprecated endpoints still fully functional; no enforcement.
- [ ] `backend/app/session/checkpointer.py:78-79` — write failure logged but swallowed; next graceful shutdown loses that session.

### Performance

- [ ] `src/hooks/useLayerWidgets.ts:13-15` — fresh `Set` per render; consumers see new identity each time.
- [ ] `src/hooks/useParamProvenance.ts:59-73` — O(widgets × bindings) per render; no precomputed index.
- [ ] `src/hooks/useAutoTetherAiSuggestions.ts:19-30` — selector joins all matching IDs into one big string key; churns the effect.
- [ ] `src/hooks/useNodePreview.ts:103-105` + `useOutputPreview.ts:45-64` — both build `draw` callbacks with too-wide deps that re-create on each render → effects re-fire.
- [ ] `src/components/widget/HslWidgetBody.tsx:26, 55, 61` / `HslSectionBody.tsx:30` / `CurvesSectionBody.tsx:71` — handler functions defined in render body and passed as props; no `useCallback`.
- [ ] `src/components/panels/LayersPanel.tsx:137-155` — no virtualisation; OK at 20 layers, painful at 200.
- [ ] `src/shaders/pipeline.ts:445-487` — single-pass adjustments still force ping-pong write; 2× bandwidth on simple chains.
- [ ] `src/shaders/pipeline.ts:160-595` — `gl.getUniformLocation` looked up every frame; should cache at program-link time.
- [ ] `src/lib/curves.ts:54-63` — O(n) per-sample segment search for the 256-entry LUT; binary search trivial.
- [ ] `backend/app/services/anthropic_client.py:460ff` — only ephemeral prompt cache; system prompts and image-context blocks are reused but not marked persistent → ~50% input-token overhead.

### Architecture / Duplication

- [x] `src/store/backend-state-slice.ts` — stores frontend-only UI state (`pendingSuggestionIds`, `previewingSuggestionIds`) inside the slice that mirrors the backend snapshot. Doctrine breach. **Fix landed:** moved `pendingSuggestionIds`, `previewingSuggestionIds`, and `acceptedSuggestions` (plus their actions) into a new `useSuggestionsUi` slice. `useBackendState` now only carries snapshot-mirroring state. The `widget.created` SSE handler still bridges into `useSuggestionsUi.markPending` for autonomous-origin widgets (cross-store call; audit C8 sweeps these together later).
- [ ] `src/core/document.ts` — undo/redo dispatches to backend first, falls back to frontend history. Workspace-only ops (node drag) have no backend rep, so the dual stack fragments user-perceived undo.
- [ ] `src/lib/pipeline-manager.ts` vs. `src/lib/image-node-renderer.ts` — dual ownership of WebGL state + render orchestration; renderer hard-couples to the global PipelineManager singleton.
- [ ] `src/hooks/useImageContext.ts:169-332` vs. `useImageContextSnapshot:380-382` — image context kept in AI session store AND in backend-state snapshot. Two stores, no sync rule.
- [ ] `src/hooks/useFileIO.ts` / `useCanvasZoom.ts` — dispatch custom DOM events (`workspace:zoom*`) listened to by `CanvasWorkspace`. Implicit, hard-to-trace coupling.
- [ ] `src/hooks/useImageNodeRender.ts:85-87, 186-209` — renderer is aware of `pendingSuggestionIds` / `previewingSuggestionIds`; couples rendering to suggestion-UI state.
- [ ] `backend/app/tools/widgets/set_param.py` vs. `set_widget_param.py` — both edit canonical params, different semantics, both "REST-only", relationship undocumented.
- [x] `backend/app/api/session.py` vs. `mcp/session.py` vs. `services/session_store.py` vs. `tools/atomic/create_session.py` — **four** session surfaces with overlapping responsibilities; unclear SSoT for session identity. **Resolved:** investigation found the actual overlap was narrower than the audit framing — `mcp/session.py` is wire-layer pairing (not lifecycle), `session_store.py` is the uncontested SSoT, and the two genuine entry points (REST `POST /api/session` and MCP `create_session` tool) diverged on payload validation: the MCP path bypassed both `max_image_bytes` and the `image/*` MIME guard the REST path enforced. New `services/image_validation.py` centralises the check (raises `ImageValidationError` with an HTTP-status hint); both create paths consume it and translate to their respective error envelopes. Module-level docstrings added to all four `session*` files documenting the responsibility split so new readers don't have to trace imports. Regression tests: MCP path rejects non-image MIME (415) and oversize payloads (413) just like REST does.
- [x] `backend/app/config/{env,runtime,ui,domain,app_config}.py` — five overlapping config modules; aggregator pattern but `domain.py` is a thin wrapper. **Fix landed:** `domain.py` had zero production callers (grep-verified) and was just a 34-line delegation to `app.registry.loader`. Deleted; `__init__.py` docstring updated to note that op param ranges come from `load_registry()` directly. Now 4 cleanly-separated modules: env (operator overrides), runtime (constants), ui (numeric tokens), app_config (aggregator).
- [ ] `backend/scripts/migrate_fused_to_presets.py` — one-shot migration kept "for auditability"; blocks deletion of `tools/fused/`.
- [x] No error boundaries anywhere in the React tree (e.g. `EditorProvider`) — a single child throw nukes the whole editor. **Resolved:** new `src/components/ui/ErrorBoundary.tsx` primitive (React 19 still needs class-based boundaries); wrapped at three strategic surfaces — App root (last-resort fallback with Reload), RightSidebar > Inspector, and per-ImageNode via a thin wrapper in `CanvasWorkspace.nodeTypes`. 5 unit tests assert containment, retry, custom-fallback, and `onError` telemetry hook.
- [ ] `shared/` contracts (types + schemas) are informational only; not codegen-validated at runtime. Drift goes undetected until the wire breaks.
- [ ] `backend/app/tools/widgets/propose_stack.py:186-189` — default-merge uses `params.get(key, p.default)`; explicit `None` value beats the default unexpectedly.

---

## 🟦 LOW

(Truncated to themes — full per-file list in the agent transcripts archived in `/private/tmp/claude-501/.../tasks/`.)

- [~] **GL state hygiene** — partial in `b48dea8`. Mask texture at TEXTURE5 (`pipeline.ts:555-570`) and 3D-LUT at TEXTURE1 (`:407-411`) still bound across passes; explanatory comments added but no actual unbinds. No early-out when adjustment list is empty (`:434-442`) — still routes through a neutral basic-shader pass.
- [ ] **Mask iteration**: `mask-overlap.ts`, `mask-outline.ts`, `mask-centroid.ts`, `overlay-painters.ts:146-199 & 212-277` all hand-roll the same `for y for x` loop. One iterator covers all.
- [ ] **Blend modes**: defined in `shaders/blend.glsl.ts` AND mirrored in `lib/layer-compositor.ts:10-19`; risk of drift.
- [ ] **Registry singletons** (`processing-registry`, `canvas-tool-registry`, `lut-registry`, `llm-tool-registry`) share the exact Map-based shape; generic `Registry<T>` would dedupe.
- [ ] **Keyboard / menu / palette action layers** maintain parallel definitions of the same commands; source files acknowledge it.
- [x] `src/lib/widget-anchor.ts:8` — `kind:'image_node'` maps to `{kind:'global'}` silently dropping `imageNodeId`/`layerIds`. **Fix landed in `977bedc`:** docstring now explains the deliberate coarse mapping.
- [x] `src/lib/auto-tune.ts:81` — undefended destructure of `m.castDirection`. **Fix landed in `977bedc`:** `?? [0, 0]` default.
- [x] `src/lib/downscale-for-upload.ts:5-9` — zero source dimensions → `Infinity / 0` → 0×0 canvas → undefined behavior. **Fix landed in `977bedc`:** throws on `<= 0` dims.
- [x] `src/lib/ai-client.ts:12` — `await response.text()` in error branch; body consumed once, fragile if logged again. **Fix landed in `977bedc`:** `readBodyOnce` helper reads + JSON-parses once and serves both error log and consumer.
- [x] `src/components/workspace/ImageNodeObjectsLayer.tsx:51` — hardcoded `#ffffff` stroke; should be token. **Fix landed in `977bedc`:** kept untokenised (black+white pair guarantees contrast on arbitrary image content); comment explains why.
- [ ] `src/components/panels/LayersPanel.tsx:234-239` — inline IIFE returning JSX; lint rule could be tightened.
- [x] `src/components/inspector/widget/primitives/CurveEditor.tsx:56-59` — non-null assertion on ref; replace with guard. **Fix landed in `977bedc`:** `svgToPoint` takes the SVG from `e.currentTarget` instead of a ref; dead `svgRef` removed.
- [ ] `tools/eslint-rules/no-nested-component-definition.js` — only catches uppercase identifiers; lowercase helpers slip past.
- [ ] `vite.config.ts:12-28` — custom `/ort/*` middleware; will break if `onnxruntime-web` changes its load paths.
- [ ] Multiple thin hook wrappers add no value: `useWorkspaceSelection`, `useHoveredWidget`, `useWidgetExpansion`.
- [ ] `backend/app/services/anthropic_client.py` — log shape inconsistent; cache-stat emission depends on the `active_doc` context-var being set, undocumented.
- [ ] `backend/app/registry/loader.py:116-120` — `_cached` never invalidated; no dev-mode hot-reload of registry JSON.
- [ ] `backend/app/tools/base.py:15-37` — `ToolPermissions` lacks an `idempotent` flag; history engine relies on ad-hoc knowledge.

---

## 🔁 Cross-cutting themes

1. **SSoT drift**: image_context (singleton vs. per-node) ✅ closed, undo snapshot capture ✅ closed, two acceptance paths ✅ closed (H4), two session surfaces ✅ closed (`d6a197c`), frontend-state shadows of backend snapshot ✅ closed (`useSuggestionsUi` cluster), engine view ↔ SSoT registry ✅ closed (`527c2b2`), widget topic-folder boundary ✅ closed (H25 wave), custom DOM-event coupling ⬜.
2. **Concurrency hygiene**: `with_document_lock` on snapshot read + replay capture ✅ closed (C9); no per-op timeout on resolver gather ⬜; EventBus race on cleanup ✅ closed; in-flight Anthropic calls not deduped ⬜; debounce-ref cleanup missing in 4 hooks ✅ closed; widget slider write floods backend ✅ closed (`2ce7234`).
3. **WebGL / render pipeline**: per-frame texture/FBO churn ✅ closed (curves LUTs, source upload, FBO resize, downscale memo), uniform-location lookups ⬜, hard-coded clarity radius ⬜, BT.601/BT.709 mismatch ✅ closed, ping-pong without short-circuit ⬜, pipeline-manager + image-node-renderer dual ownership ⬜.
4. **Duplication ripe to collapse**:
   - [x] 4 param hooks → 1 (H20) — `use-param.ts` core landed; widget-path debounce + 429 swallow follow-up landed in `2ce7234`; `RegistryDrivenSectionBody` toolrail stale-write guard still open.
   - [x] 17 fused-tool files → base class (H21) — 7 of 9 overrides removed; 2 legitimate special cases documented.
   - [ ] 4 registry singletons → generic (Low)
   - [x] `propose_widget` + `propose_stack._handle_tool_invoked` → one (H24) — single entry point now.
   - [ ] `set_param` + `set_widget_param` → one (M)
   - [x] `tools_rest` + `mcp.tools/call` dispatch → one with shared rate limit (C5+M) — rate limiter shared in `83a3668`; full dispatch unification still open.
   - [ ] shortcuts / menu / palette actions → one source of truth (Low)
   - [x] `spawnToolWidget` + `_resolveSpawnContext` → one (H22) — toolrail spawn gate consolidated in `2a88a57`.
   - [x] Inspector-folder primitives leaking into `widget/` (H25) → primitives sorted to `ui/` vs `widget/` per CLAUDE.md rule; 6 commits ending at `b266942`.
   - [x] `engine/registry.py` (ENGINE_OPS) vs `registry/loader.py` overlap (H23 remainder) → engine view layer deleted in `527c2b2`; accessor helpers on the loader cover the two derived fields.
5. **Persistence/resource bounds**: no disk pruning ✅ closed, unbounded queues ✅ closed, unbounded history ⬜, unbounded optimistic map ⬜, ephemeral-only prompt cache ⬜.

---

## Execution log

**Mechanical wave — landed in `83a3668`, `b48dea8`, `977bedc`:**
- Backend stability: C1, C4, C5, C6, C14, H10
- WebGL correctness + perf: C11, C12, C13, H14, H15, H16, partial GL hygiene
- Frontend bugs: C7 (HslPanelView only), H1 (4 debounce cleanups)
- Hook + lib polish (low bucket): widget-anchor doc, auto-tune guard, downscale-for-upload guard, ai-client body cache, hardcoded color → doc, CurveEditor non-null removed.

**Per-node migration coherence cluster — landed in `c320538..ae701bc`:**
- C2 + C3 + H8 + H9, plus `_new_document` migrated to per-node bytes, doctrine docstrings in 4 modules, fixture sweep.

**Remaining focused-plan clusters (each its own session):**
- ~~Param-hook merge (H20)~~ — done; only the `RegistryDrivenSectionBody` toolrail-path stale-write guard remains.
- ~~Fused-tool base class (H21)~~ — done.
- ~~Registry consolidation + `propose_widget` deletion (H23 + H24)~~ — done; engine view also collapsed.
- ~~Session-surface SSoT (4-way merge)~~ — done in `d6a197c`; shared `image_validation.py` used by both create paths.
- ~~Selection / acceptance path unification (H4 + frontend UI state out of backend slice)~~ — done in the `useSuggestionsUi` slice cluster (`b332105..d576f7f`).

**Audit-cleanup wave landed 2026-06-15 evening:**
- H22 (toolrail-spawn dedupe) — `2a88a57`
- H23 remainder (engine/registry.py collapse) — `7e225ce`, `f6c64b2`, `28728a8`, `527c2b2` (+ shape tests bundled into `d6a197c` by parallel commit)
- H25 (widget/inspector primitive boundaries) — 6 commits `cc603cb` → `b266942`
- H20 follow-up (widget-path slider write rate-limit + 429 console spam) — `2ce7234`

**Open standalone items worth a single mechanical wave:** H2, H3, H5, H6, H7, H11, H12, H13, H17, H18, H19, H26 + the entire Medium bucket + remaining Low. (C8 partial pieces, C9, C10 already landed.)

---

## Per-agent transcripts

Archived at `/private/tmp/claude-501/-Users-anton-Dev-Projects-editor/<run-id>/tasks/` — full per-file rationale for each finding above.
