{
  "$defs": {
    "GlobalScope": {
      "additionalProperties": false,
      "properties": {
        "kind": {
          "const": "global",
          "enum": [
            "global"
          ],
          "title": "Kind",
          "type": "string"
        }
      },
      "required": [
        "kind"
      ],
      "title": "GlobalScope",
      "type": "object"
    },
    "ImageNodeScope": {
      "additionalProperties": false,
      "properties": {
        "imageNodeId": {
          "minLength": 1,
          "title": "Imagenodeid",
          "type": "string"
        },
        "kind": {
          "const": "image_node",
          "enum": [
            "image_node"
          ],
          "title": "Kind",
          "type": "string"
        },
        "layerIds": {
          "items": {
            "type": "string"
          },
          "title": "Layerids",
          "type": "array"
        }
      },
      "required": [
        "kind",
        "imageNodeId"
      ],
      "title": "ImageNodeScope",
      "type": "object"
    },
    "MaskScope": {
      "additionalProperties": false,
      "properties": {
        "kind": {
          "const": "mask",
          "enum": [
            "mask"
          ],
          "title": "Kind",
          "type": "string"
        },
        "maskId": {
          "minLength": 1,
          "title": "Maskid",
          "type": "string"
        }
      },
      "required": [
        "kind",
        "maskId"
      ],
      "title": "MaskScope",
      "type": "object"
    },
    "NamedRegionScope": {
      "additionalProperties": false,
      "properties": {
        "kind": {
          "const": "named_region",
          "enum": [
            "named_region"
          ],
          "title": "Kind",
          "type": "string"
        },
        "label": {
          "minLength": 1,
          "title": "Label",
          "type": "string"
        }
      },
      "required": [
        "kind",
        "label"
      ],
      "title": "NamedRegionScope",
      "type": "object"
    },
    "ReplicateScope": {
      "additionalProperties": false,
      "description": "Apply one op's params to each target (imageNode, layer) independently.\n\nDistinct from ImageNodeScope's composite semantic \u2014 replicate never merges\nlayers; the same params land on every target layer separately, and targets\nmay span multiple image nodes.",
      "properties": {
        "kind": {
          "const": "replicate",
          "enum": [
            "replicate"
          ],
          "title": "Kind",
          "type": "string"
        },
        "targets": {
          "items": {
            "$ref": "#/$defs/ReplicateTarget"
          },
          "title": "Targets",
          "type": "array"
        }
      },
      "required": [
        "kind"
      ],
      "title": "ReplicateScope",
      "type": "object"
    },
    "ReplicateTarget": {
      "additionalProperties": false,
      "properties": {
        "imageNodeId": {
          "minLength": 1,
          "title": "Imagenodeid",
          "type": "string"
        },
        "layerId": {
          "minLength": 1,
          "title": "Layerid",
          "type": "string"
        }
      },
      "required": [
        "imageNodeId",
        "layerId"
      ],
      "title": "ReplicateTarget",
      "type": "object"
    }
  },
  "description": "Discriminated union over the scope kinds.",
  "discriminator": {
    "mapping": {
      "global": "#/$defs/GlobalScope",
      "image_node": "#/$defs/ImageNodeScope",
      "mask": "#/$defs/MaskScope",
      "named_region": "#/$defs/NamedRegionScope",
      "replicate": "#/$defs/ReplicateScope"
    },
    "propertyName": "kind"
  },
  "oneOf": [
    {
      "$ref": "#/$defs/GlobalScope"
    },
    {
      "$ref": "#/$defs/NamedRegionScope"
    },
    {
      "$ref": "#/$defs/MaskScope"
    },
    {
      "$ref": "#/$defs/ImageNodeScope"
    },
    {
      "$ref": "#/$defs/ReplicateScope"
    }
  ],
  "title": "Scope"
}
